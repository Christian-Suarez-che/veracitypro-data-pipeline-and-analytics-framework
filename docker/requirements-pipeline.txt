# Airflow Providers (required for Astro pipeline)
apache-airflow-providers-snowflake>=5.6.0
apache-airflow-providers-amazon>=8.26.0
apache-airflow-providers-http>=4.12.0
apache-airflow-providers-slack>=8.7.0
apache-airflow-providers-airbyte>=3.8.0

# Cosmos for dbt integration
astronomer-cosmos>=1.5.0

# dbt dependencies (for Cosmos to invoke dbt)
dbt-core>=1.7.0,<2.0.0
dbt-snowflake>=1.7.0,<2.0.0

# Data processing libraries
pandas>=2.1.0
numpy>=1.24.0
scipy>=1.11.0
pyarrow>=14.0.0

# AWS and Snowflake connectors
boto3>=1.34.0
snowflake-connector-python[pandas]>=3.6.0
snowflake-sqlalchemy>=1.5.0

# Utilities
requests>=2.31.0
fastjsonschema>=2.19.0

# Development/Testing (optional in production, but useful for local validation)
pytest>=7.4.0
ruff>=0.1.0
